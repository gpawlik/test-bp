import { css } from "styled-components/native";
import { CIRCLE_SIZE } from "../../constants";

export const bubbleBackgroundStyles = css`
  position: absolute;
  height: ${CIRCLE_SIZE}px;
  width: ${CIRCLE_SIZE}px;
`;
